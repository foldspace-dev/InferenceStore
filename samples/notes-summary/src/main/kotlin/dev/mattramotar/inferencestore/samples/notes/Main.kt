package dev.mattramotar.inferencestore.samples.notes

import dev.mattramotar.inferencestore.core.InferenceStore

fun main() {
    println("InferenceStore notes-summary sample — core ${InferenceStore.VERSION}")
    println("The real local-first / cloud-fallback / privacy demo lands with OSS-31.")
}

package dev.mattramotar.inferencestore.provider.openai

/**
 * Placeholder for the OpenAI-compatible cloud adapter (OSS-24).
 *
 * The Ktor-backed streaming implementation, API-key handling, and error
 * mapping land with that issue. This marker keeps the module in the graph.
 */
public object OpenAiCompatibleProvider {
    public const val ID: String = "openai-compatible"
}
